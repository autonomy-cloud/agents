# OpenAgents OpenAI-compatible plugin

Internal LLM, transcription, and speech endpoints using the OpenAI-compatible
HTTP protocol. Public provider endpoints are rejected by default.

```bash
pip install openagents-plugin-openai
```
